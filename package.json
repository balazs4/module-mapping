{
  "name": "module-mapping",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "esbuild --minify --bundle --target=chrome91 index.js"
  },
  "dependencies": {
    "ajv": "^8.6.1",
    "ajv-formats": "^2.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.12.15"
  }
}
