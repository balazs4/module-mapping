{
  "name": "module-mapping",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "esbuild --minify --bundle --target=chrome91 --external:util index.js"
  },
  "dependencies": {
    "ajv": "^8.6.1",
    "ajv-formats": "^2.1.0",
    "ajv-formats-draft2019": "^1.4.3"
  },
  "devDependencies": {
    "esbuild": "^0.12.15"
  }
}
